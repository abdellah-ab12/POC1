apiVersion: v1
kind: Service
metadata:
  name: simplegreetingapi-svc
  namespace: poc3
spec:
  type: NodePort
  selector:
    app: simplegreetingapi
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
